from typing import TypeVar, TypeAlias, Generic, Optional, Iterable, Iterator, TypeGuard
from typing import cast, overload
from typing import Any
from collections.abc import Sequence, MutableSequence, Sized

T = TypeVar('T') # Template parameter for generic classes or functions
